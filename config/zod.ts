// import { z } from 'zod';

// z.setErrorMap((issue, ctx) => {
//   switch (issue.code) {
//     case 'too_small':
//       return {
//         message: `O valor informado deve ser ${issue.minimum}`,
//       };

//     default:
//       return { message: issue.message };
//   }
// });

// export { z };
